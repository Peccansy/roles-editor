const initialState = [{ id: 1, name: 'Бухгалтер' }];

const rolesReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default rolesReducer